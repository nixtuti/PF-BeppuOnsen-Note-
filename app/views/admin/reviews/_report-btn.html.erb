<% if review.report == true %>
  <%= link_to "通報を解除", admin_review_report_path(review), method: :patch, remote: true, class: "btn btn-sm btn-danger" %>
<% else %>
  <%= link_to "通報を解除", admin_review_report_path(review), method: :patch, remote: true, class: "btn btn-sm disabled btn-danger" %>
<% end %>